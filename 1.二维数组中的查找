# -*- coding:utf-8 -*-
class Solution:
    # array 二维列表
    def Find(self, target, array):
        # write code here 
        rows=len(array)-1
        i=0
        while i<=rows:
            if target in array[i]:
                return True
            else:
                i += 1
        return False
